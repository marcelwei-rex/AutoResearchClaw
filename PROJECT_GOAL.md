# AutoResearchClaw 项目目标

## 核心目标

本项目的目标是：

> 当研究者已经拥有明确、具有研究价值的 idea，并拥有 Trust-HUB 等可信公开数据
> 或研究者自己的真实实验数据时，通过 AutoResearchClaw 工作流，在研究者主导下，
> 结合 Codex、Kimi 等模型的生成、核查和独立复核能力，产出一篇证据可追溯、
> 实验可复现、论证完整、达到至少中科院二区目标期刊通常要求的投稿候选论文。

“达到中科院二区要求”是本项目追求的论文质量门槛，不代表系统能够保证期刊分区、
审稿结果或最终录用。期刊分区和审稿标准会变化，最终选刊、学术判断、署名与投稿
责任仍由研究者承担。

## 必要输入

工作流启动前，至少应具备：

1. 由研究者确认的明确研究问题、创新点和适用边界；
2. 合法、真实、可追溯的数据来源，例如 Trust-HUB 或研究者自己的实验数据；
3. 可执行并可复核的实验设计、评价指标和对照方法；
4. 足以支持主要论文主张的实验结果，而不是由模型生成或补齐的虚构证据。

缺少上述输入时，工作流应明确停止、降级为探索性草稿，或报告证据不足，不得通过
放宽门禁、猜测结果或伪造引用来制造“可投稿”状态。

## 工作流的职责

AutoResearchClaw 及参与模型用于：

- 将研究 idea 转化为可审计的研究问题、实验计划和论文结构；
- 对真实数据和实验产物执行确定性捕获、校验、重放和引用绑定；
- 生成有界的论文草稿、图表、LaTeX、参考文献和投稿材料；
- 通过 Codex、Kimi 等不同模型进行角色隔离的生成、批判和独立复核；
- 在发现证据、引用、语义或发布生命周期问题时 fail-closed，并保留可诊断信息；
- 帮助研究者提高论文生产的效率、一致性和可审计性。

模型不得成为实验事实、指标、数据来源、引用关系或录用等级的最终权威。

## 成功标准

一次运行只有同时满足以下条件，才可进入“二区目标投稿候选”人工终审：

1. **研究真实性**：idea、数据、实验和主要结论均来自真实且获授权的研究输入；
2. **可复现性**：关键代码、配置、数据身份、执行记录和指标计算可独立重放；
3. **证据闭包**：正文中的实验事实、数值、比较关系和引用均与 canonical authority
   严格绑定；
4. **论文完整性**：问题、方法、实验、结果、局限、相关工作和结论形成完整论证；
5. **质量审查**：至少经过一次独立 Codex 代码/证据审查，以及必要时的 Kimi、
   Claude 或其他模型交叉评审；
6. **发布闭包**：Stage 1–25 fresh run、independent reconstruction、
   semantic checks 和 `release_check` 按目标 scope 通过；
7. **人工终审**：研究者确认科学贡献、结果解释、选刊适配、伦理合规、署名和
   投稿材料。

单次测试通过、单次 F0 绿灯或模型给出高分，都不能单独证明论文已经达到目标质量。

## 工程决策原则

- 论文可信度优先于全自动化率和单次运行成功率。
- 真实证据和可复现性优先于文稿表面完整。
- 对复杂、低把握或跨 authority 边界的修改，先停下来进行多 Agent 独立复核。
- 修复应优先消除结构性矛盾；若连续失败只产生更多特例或正则补丁，应暂停并重新
  评估架构，而不是无限堆叠局部规则。
- 失败运行是发现真实契约缺口的证据，但每项修复都必须证明它提高了通用正确性，
  而不只是让某一个历史 run 通过。

## 当前阶段性目标

在扩大领域或追求无人值守运行前，先完成：

1. 使用固定代码和配置完成 fresh、no-resume 的真实 Stage 1–25 验收；
2. 对同一支持场景连续获得可复现的完整通过结果；
3. 由独立 reviewer 确认论文事实、引用、实验和发布 authority 未被放宽；
4. 对生成论文进行人工学术质量评估，并与目标中科院二区期刊的最新作者指南和
   代表性论文进行逐项差距分析。

本文件是项目的长期目标说明。具体 stage 契约、实现规范和 release gate 仍以相应
版本化规范与生产代码为准。
